// parts SQL

// select all parts

/*

conn.query("SELECT * FROM `parts`").then((rows) => {
    console.log(rows);
});


// Select unique part models from parts table

conn.query("SELECT DISTINCT `model` FROM `parts`").then((rows) => {
    console.log(rows);
});

// select unique part producer from parts table

conn.query("SELECT DISTINCT `producer` FROM `parts`").then((rows) => {

    console.log(rows);
});


// select all parts by producer from date_from to date_to

conn.query("SELECT * FROM `parts` WHERE `producer` = 'producer' AND `date` BETWEEN 'date_from' AND 'date_to'").then((rows) => {

    console.log(rows);
}
);

// select all parts by model from date_from to date_to

conn.query("SELECT * FROM `parts` WHERE `model` = 'model' AND `date` BETWEEN 'date_from' AND 'date_to'").then((rows) => {
    console.log(rows);
}
);

// select all parts by producer and model from date_from to date_to

conn.query("SELECT * FROM `parts` WHERE `producer` = 'producer' AND `model` = 'model' AND `date` BETWEEN 'date_from' AND 'date_to'").then((rows) => {
    console.log(rows);
});
*/